language: python
python:
  - "3.7"
  - "3.8"      # current default Python on localhost

# command to install dependencies
install:
  - pip install -r requirements.txt

# command to run tests
script:
  python3 IoT_access_native_JSON.py data/geolocation_ch.csv